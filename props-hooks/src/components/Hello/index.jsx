import React from 'react'

const Hello = (props) => {
    // props.info();
    // console.log(props.showInfo())
    // const info = props.showInfo();
    
  return (
    <div>
      {/* Hi ,{info} */}
    </div>
  )
}

export default Hello
